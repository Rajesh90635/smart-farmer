# Seed data

Reserved for development seed-data scripts (e.g. sample dealers/products
for the marketplace module). None exist yet — no business schema exists
yet either. Will be populated alongside the modules that need seed data
for local testing.
