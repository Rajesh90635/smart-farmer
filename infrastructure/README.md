# Infrastructure

Reserved for future infra configuration (e.g. a reverse proxy config for
pilot deployment, or Kubernetes manifests if that's ever needed). Empty in
the foundation phase - docker-compose.yml at the repo root covers all
local-development orchestration for now.
