# Cross-cutting tests

Reserved for integration/E2E tests that span backend + AI service +
mobile together (e.g. "upload a photo end-to-end and see a diagnosis").
None exist yet since no business module exists yet. Per-project unit/
integration tests live in backend/tests/, ai/tests/, and mobile/test/.
