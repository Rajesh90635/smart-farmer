# Development-only tooling (not required to run the app, only to develop it)
-r requirements.txt

ruff==0.6.9        # lint + format (replaces separate flake8/black/isort)
mypy==1.11.2       # static type checking
